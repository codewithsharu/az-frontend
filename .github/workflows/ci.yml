name: React Build

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: codewithsharu/templates/.github/workflows/react/build/build.yml@main
    with:
      node-version: '18'
      build-folder: 'build'
      artifact-name: 'react-build'
