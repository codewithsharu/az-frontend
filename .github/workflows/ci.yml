name: React Build

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: codewithsharu/templates/.github/workflows/build.yml@main
    with:
      artifact-name: react-build
      build-folder: build
